# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SharedTasks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SharedTasks
  pod 'RealmSwift'
  pod 'RealmLoginKit'
  pod 'Fakery'
  pod 'Eureka'
  pod 'ImageRow'
  pod 'Alertift'

  target 'SharedTasksTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SharedTasksUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
